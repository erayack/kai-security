[pytest]
# Limit collection to this repo's test suite.
testpaths =
    tests

# Prevent recursive collection in heavy / generated / vendored directories.
norecursedirs =
    testbed
    output
    .venv
    venv
    node_modules
    dist
    build
    __pycache__

addopts =
    -ra

asyncio_mode = auto
