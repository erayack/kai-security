# Kai Evaluation

## Blackbox Evaluation

Make the file `evaluation/eval_blackbox.sh` executable:

```bash
chmod +x evaluation/eval_blackbox.sh
```

and run it:
```bash
./evaluation/eval_blackbox.sh
```

This will run the blackbox evaluation pipeline and save the results to the `evaluation_output` directory.